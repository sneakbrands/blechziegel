<style>
  .bz-products-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }

  .bz-product-card {
    background: #ffffff;
    border: 1px solid rgba(var(--bz-navy-rgb), 0.08);
    border-radius: 14px;
    overflow: hidden;
    position: relative;
    display: flex;
    flex-direction: column;
    transition: transform .22s ease, box-shadow .22s ease, border-color .22s ease;
    box-shadow: 0 8px 24px rgba(var(--bz-navy-rgb), 0.05);
  }
  .bz-product-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 18px 42px rgba(var(--bz-navy-rgb), 0.12);
    border-color: rgba(var(--bz-orange-rgb), 0.32);
  }

  .bz-product-img {
    aspect-ratio: 1 / 1;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8px;
    background:
      radial-gradient(circle at top left, rgba(255,255,255,0.95) 0%, rgba(255,255,255,0) 32%),
      linear-gradient(180deg, #f8f7f4 0%, #efede8 100%);
    border-bottom: 1px solid rgba(var(--bz-navy-rgb), 0.05);
  }
  .bz-product-img::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.18) 0%, rgba(255,255,255,0) 45%);
    pointer-events: none;
  }
  .bz-product-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    padding: 0;
    position: relative;
    z-index: 1;
    filter: drop-shadow(0 16px 24px rgba(0, 0, 0, 0.08));
    transition: transform .28s ease, filter .28s ease;
  }
  .bz-product-card:hover .bz-product-img img { transform: scale(1.045); filter: drop-shadow(0 18px 28px rgba(0, 0, 0, 0.12)); }

  .bz-product-img-link, .bz-product-name-link { color: inherit; text-decoration: none; display: block; }
  .bz-product-name-link:hover .bz-product-name,
  .bz-product-name-link:focus-visible .bz-product-name { text-decoration: underline; text-underline-offset: 3px; }
  .bz-product-img-link:focus-visible, .bz-product-name-link:focus-visible {
    outline: 2px solid var(--bz-navy); outline-offset: 2px; border-radius: 8px;
  }

  .bz-product-badge {
    position: absolute; top: 14px; left: 14px; z-index: 2;
    display: inline-flex; align-items: center; gap: 6px;
    padding: 7px 12px; border-radius: 999px;
    background: rgba(var(--bz-orange-rgb), 0.96); color: var(--bz-navy);
    font-size: 12px; font-weight: 700; letter-spacing: .02em;
    box-shadow: 0 6px 14px rgba(var(--bz-orange-rgb), 0.22);
  }

  .bz-product-body { padding: 20px 18px 18px; flex: 1; display: flex; flex-direction: column; gap: 10px; }

  .bz-product-dachtyp {
    display: inline-flex; align-items: center; gap: 6px;
    align-self: flex-start;
    padding: 5px 10px; border-radius: 999px;
    background: #fff3df; color: var(--bz-navy);
    font-size: 11.5px; font-weight: 700; letter-spacing: .01em;
    border: 1px solid rgba(var(--bz-orange-rgb), 0.28);
  }

  .bz-product-name { color: var(--bz-navy); font-weight: 700; font-size: 16px; line-height: 1.3; letter-spacing: -0.01em; margin-bottom: 0; }

  .bz-product-passend {
    align-self: flex-start;
    padding: 5px 10px; border-radius: 999px;
    background: #f0f4fa; color: var(--bz-navy);
    font-size: 11.5px; font-weight: 600;
    border: 1px solid rgba(var(--bz-navy-rgb), 0.08);
  }

  .bz-product-desc { font-size: 13px; color: var(--bz-gray-text); flex: 1; line-height: 1.55; }

  .bz-product-price { color: var(--bz-navy); font-size: clamp(1.55rem, 2vw, 1.9rem); font-weight: 800; line-height: 1; letter-spacing: -0.02em; font-family: var(--font-heading-family, inherit); }
  .bz-product-price-sub { font-size: 11.5px; color: var(--bz-gray-text); margin-top: 4px; }

  .bz-product-trustline {
    display: flex; flex-wrap: wrap; align-items: center; gap: 6px;
    margin-top: 4px; padding: 10px 12px;
    background: #f8f7f4; border: 1px solid rgba(var(--bz-navy-rgb), 0.06); border-radius: 12px;
    cursor: default; transition: background .15s ease, border-color .15s ease;
  }
  .bz-product-card:hover .bz-product-trustline { background: #f5f4f1; border-color: rgba(var(--bz-navy-rgb), 0.08); }
  .bz-product-trustitem { color: var(--bz-navy); font-size: 12.5px; font-weight: 600; line-height: 1.4; }
  .bz-product-trustitem:first-child { font-weight: 700; }
  .bz-product-trustdot { color: rgba(var(--bz-orange-rgb), 0.9); font-weight: 700; line-height: 1; }

  .bz-product-cta { margin-top: 2px; }
  .bz-product-card .bz-btn-primary {
    width: 100%; justify-content: center; min-height: 54px; border-radius: 14px;
    background: linear-gradient(180deg, var(--bz-orange) 0%, #eb9d1e 100%); color: var(--bz-navy);
    font-weight: 700; font-size: 1rem; border: none;
    box-shadow: 0 10px 20px rgba(var(--bz-orange-rgb), 0.18);
    transition: transform .18s ease, box-shadow .18s ease, background .18s ease;
    display: inline-flex; align-items: center;
  }
  .bz-product-card .bz-btn-primary:hover { transform: translateY(-1px); box-shadow: 0 14px 24px rgba(var(--bz-orange-rgb), 0.24); background: linear-gradient(180deg, #f7ad34 0%, #eea225 100%); }

  @media (max-width: 960px) { .bz-products-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 640px) {
    .bz-products-grid { grid-template-columns: 1fr; }
    .bz-product-card { border-radius: 12px; }
    .bz-product-img { padding: 6px; }
    .bz-product-img img { padding: 0; }
    .bz-product-body { padding: 16px 14px 14px; }
    .bz-product-trustline { gap: 5px; padding: 8px 10px; }
    .bz-product-trustitem { font-size: 11.5px; }
    .bz-product-card .bz-btn-primary { min-height: 50px; border-radius: 12px; }
    .bz-product-price { font-size: 1.6rem; }
  }
</style>
